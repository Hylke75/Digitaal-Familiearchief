# Test fixtures

Synthetic-only fixtures (CLAUDE.md §56). Generators produce mock media/documents with no real personal information. Large binaries (*.bin) are gitignored; commit the generators, not the payloads.
