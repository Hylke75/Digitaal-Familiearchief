# Hoist nothing implicitly; rely on explicit workspace deps.
# Next.js and its plugins need their transitive deps resolvable, so allow hoisting patterns commonly required.
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=*prettier*
public-hoist-pattern[]=*types*
strict-peer-dependencies=false
auto-install-peers=true
