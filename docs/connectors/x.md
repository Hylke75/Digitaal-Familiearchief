# X connector

_Type: ARCHIVE_IMPORT. Re-verify API terms/pricing at build._

**WHAT WORKS** — guided import of the official X **account archive** ZIP (posts,
media, metadata). Auto-detected on upload.

**REQUIRES PROVIDER APPROVAL** — a live user-auth API route (if used) sits behind
paid X API tiers; only implement behind a capability flag once verified.

**NOT AVAILABLE** — complete ongoing archival via API is not promised.

**FALLBACK** — guided archive import IS the primary path.
